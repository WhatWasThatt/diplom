json.extract! harddisk, :id, :name, :description, :price, :produced, :brand_id, :category_id, :country_id, :volume, :interface, :factor, :created_at, :updated_at
json.url harddisk_url(harddisk, format: :json)
